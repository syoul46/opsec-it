// Tailwind v4 : la configuration des tokens est dans app/globals.css via @theme.
// Ce fichier est conservé pour compatibilité éventuelle mais n'est pas chargé.
import type { Config } from "tailwindcss";
const config: Config = { content: [] };
export default config;
